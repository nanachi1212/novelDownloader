# 番茄小說字型閱讀預覽

1. 啟動程式，點選「番茄小說：預覽支援」，貼上書籍頁網址並載入目錄。
2. 對目錄明確標記為公開的章節，使用「開啟原站閱讀器」在瀏覽器確認正常可讀。遇到登入、購買或驗證頁時停止，不匯入該頁。
3. 在瀏覽器使用「另存網頁，完整」保存目前章節頁，保留旁邊自動建立的 `<檔名>_files` 資料夾。不要提供 Cookie、token 或帳號資料。
4. 在程式選取相同 itemId 的章節，點選「匯入已保存閱讀頁」並挑選 HTML。程式會核對 reader URL 的 itemId；HTML 若有書籍 ID，也必須吻合目前書籍。內容及字型需在保存資源資料夾中。
5. 點選「開啟字型閱讀預覽」。預覽頁頂端會顯示字型實際載入狀態；若失敗，請回原站閱讀。原始 HTML 不會在瀏覽器中執行，程式只輸出正文段落與受控 CSS。

原始 JSON 快取與手動匯入預覽各自保存在使用者資料目錄 `preview/fanqie/<book_id>/` 下，按 `item_id` 分開。已有同一章節的匯入資料時不覆寫。字型預覽只改變畫面字形，不還原底層文字，因此複製、搜尋、朗讀和 TXT／EPUB 匯出都不能視為正確。
