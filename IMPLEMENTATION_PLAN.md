# novelDownloader 耐用性 Implementation Plan

## 現況

目前已有 HTTP 重試、20 秒 timeout、429 退避、編碼偵測、非法檔名清理、章號異常提示、章節快取、斷點續傳、跨書併發、每站併發限制、GUI 下載佇列、取消任務與分組 log。

## 本次強化

1. 目錄依章節 URL 去重，避免網站重複連結造成重複章節。
2. 章節快取使用 `.part` 暫存後原子替換；空白或損壞快取自動重抓。
3. 每本書保存 `progress.json`，記錄來源、總章數、完成章號、最後錯誤與更新時間。
4. TXT／EPUB 輸出先建立 `.part`，完成後原子替換正式檔。
5. 新增單元測試，並執行完整 pytest 與 PyInstaller 封裝。

## 完成標準

- 中斷寫入不會產生可被誤認為完成的章節或輸出檔。
- 重跑同網址會沿用有效快取，損壞快取會自動修復。
- 使用者可從 `cache/<book_id>/progress.json` 看見可恢復進度。
- 重複目錄 URL 只下載並輸出一次。
